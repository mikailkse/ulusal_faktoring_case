import 'package:flutter/material.dart';

class IconConstants {
  static const profileIcon = [Icons.person, Icons.message];
  static const preferencesIcon = [
    Icons.dashboard_customize,
    Icons.notifications,
    Icons.qr_code
  ];
  static const homeIconCard = [
    Icons.wallet,
    Icons.qr_code,
    Icons.document_scanner
  ];
}
